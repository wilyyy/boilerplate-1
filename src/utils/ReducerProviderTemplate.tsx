// AppProvider HOC to manage global state using reducer hook
// rename AppProvider.tsx if gonna use this

const PutSomethingHere: React.FC = () => {
   return <></>;
};

export default PutSomethingHere;
